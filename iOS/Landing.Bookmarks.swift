import SwiftUI

extension Landing {
    struct Bookmarks: View {
        var body: some View {
            HStack {
                Image(systemName: "app")
                Text("hello world")
                Spacer()
            }
        }
    }
}
