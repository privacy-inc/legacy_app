import SwiftUI

extension Landing {
    struct Bookmark: View {
        var body: some View {
            HStack {
                Image(systemName: "app")
                Text("hello world")
                Spacer()
            }
        }
    }
}
