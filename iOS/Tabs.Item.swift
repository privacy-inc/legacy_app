import SwiftUI

extension Tabs {
    struct Item: View {
        let index: Int
        
        var body: some View {
            Button {
                
            } label: {
                Snap(image: nil, width: 150, height: 150)
            }
        }
    }
}
