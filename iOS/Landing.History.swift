import SwiftUI

extension Landing {
    struct History: View {
        var body: some View {
            HStack {
                Image(systemName: "app")
                Text("hello world")
                Spacer()
            }
        }
    }
}
